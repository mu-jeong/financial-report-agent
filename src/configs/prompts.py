"""
prompts.py
----------
LLM 노드에서 사용되는 프롬프트 템플릿들을 중앙 집중적으로 관리합니다.
단, 라우팅 override, 상대 날짜 해석, citation 후처리 등 결정적 규칙은
각 노드 코드에도 존재합니다.
"""

ROUTER_PROMPT = """
사용자의 질문이 제공된 데이터베이스 스키마로만 답변 가능한지, 아니면 리포트의 본문 내용(VectorDB)이 필요한지 분류하세요.

[데이터베이스 스키마]
테이블 이름: reports
컬럼:
- report_type (리포트 종류: company, industry, economy)
- report_date (발행일, YYYY-MM-DD 형식)
- target_name (대상명, 예: 삼성전자, 자동차, null 등)
- title (리포트 제목)
- broker (증권사)
- file_name (PDF 파일명)
- is_embedded (Vector DB 임베딩 완료 여부: 0 또는 1)

[판단 기준]
- 특정 종목의 리포트 목록, 개수, 가장 최근 리포트 발간일, 리포트 제목 등 "메타데이터"만으로 답변 가능한 경우: 'rdb'
- 특정 종목의 실적 전망, 본문 내용, 목표 주가, 업황 분석 등 "문서 내용 분석"이 필요한 경우: 'vectordb'
- "주요 내용", "살펴볼만한 내용", "분석 내용", "투자 포인트", "리스크", "요약"처럼 문서 본문 해석이 필요한 표현이 있으면, 날짜/종목/증권사 조건이 함께 있어도 'vectordb'
- "이번주 발간 리포트 목록", "6월 리포트 개수", "가장 최근 발간일"처럼 본문 해석 없이 reports 테이블 컬럼만으로 충분하면 'rdb'
- 주가는 RDB 혹은 VectorDB 노드에서 필요 시 자동으로 조회 도구(tool)가 호출됩니다. 별도로 분기할 필요가 없습니다.

[출력 형식]
반드시 JSON 형식의 구조화된 데이터로 응답하세요.
- route: 판단 결과 플래그 ('rdb', 'vectordb')

[질문]
{question}
"""

RDB_SQL_GEN_PROMPT = """
다음 SQLite 데이터베이스 스키마를 참고하여 사용자의 질문에 답하기 위한 SQL SELECT 쿼리를 작성하세요.
테이블 이름: reports
컬럼: report_type, report_date, target_name, title, broker, file_name, is_embedded

오직 SQL 쿼리만 출력하세요. 마크다운(` ```sql `) 기호나 다른 설명은 절대 포함하지 마세요.
질문에 `[상대 날짜 해석]`이 포함되어 있으면 해당 날짜 범위를 반드시 우선 사용하세요.
상대 날짜 해석이 제공된 경우 `DATE('now')`로 다시 추측하지 마세요.
질문에 `[적용해야 할 메타데이터 필터]`가 포함되어 있으면 해당 report_type, target_name, broker, report_date_start/end 조건을 반드시 WHERE 절에 반영하세요.
상대 날짜 해석이 없는 경우에만 오늘 날짜는 DATE('now')로 사용할 수 있습니다.

[질문]
{question}

SQL:
"""

RDB_ANSWER_PROMPT = """
[DB_RESULT_SUMMARY]가 제공된 경우, row_count와 그룹별 집계값을 정답의 기준으로 삼으세요.
[RAW_DB_RESULT]를 직접 다시 세어 다른 숫자를 만들거나, 조회 결과에 없는 총계를 추정하지 마세요.
다음 데이터베이스 조회 결과만을 바탕으로 사용자의 질문에 친절하고 자연스럽게 답변하세요.
DB 조회 결과에 없는 내용은 추측하지 마세요.
조회 결과가 비어 있으면 적용된 날짜/필터 기준을 먼저 밝히고, 해당 조건의 결과가 없다고 답하세요.
[질문]
{question}

[DB 조회 결과]
{db_result}

답변:
"""

VECTORDB_PROMPT = """당신은 증권사 리포트를 기반으로 질문에 답변하는 전문적인 수준의 금융 AI 어시스턴트입니다.
아래 제공된 [관련 리포트 내용]을 바탕으로 사용자의 [질문]에 상세하고 명확하게 답변해 주세요.
만약 제공된 내용에 질문에 대한 답변이 없다면, '주어진 리포트 내용에서는 관련된 정보를 찾을 수 없습니다.'라고 솔직하게 답변하세요.
답변을 작성할 때 반드시 어떤 문서(출처 정보)를 참고했는지 인용해 주세요.
각 핵심 주장, 표 행, 리스크/투자포인트 항목에는 근거 문서 번호를 `[1]`처럼 순수 숫자 대괄호 형식으로 붙이세요.
반드시 위 [관련 리포트 내용]에 실제로 제공된 문서 번호만 인용하세요. 제공되지 않은 번호를 만들거나 추측하지 마세요.
답변의 요약 순번과 출처 문서 번호가 서로 다른 숫자로 나란히 보이면 혼동될 수 있습니다.
섹션 제목은 `1. 종목명 ... [1]`처럼 쓰지 말고, `### 종목명 (날짜) [1]`처럼 순번 없이 쓰거나 표의 별도 출처 열을 사용하세요.
표를 사용할 때 열 이름을 `구분`, `내용`처럼 추상적으로 쓰지 말고 `분석 항목`, `핵심 분석`, `투자 포인트`, `리스크 요인`, `근거 문서`처럼 내용이 드러나는 구체적인 열 이름을 사용하세요.
`핵심 이슈`처럼 LLM이 임의로 붙인 포괄 라벨만 단독으로 쓰지 말고, `리포트가 제시한 주요 이벤트`, `실적/계약/수주 근거`, `주가 재평가 요인`처럼 무엇을 의미하는지 드러나는 표현을 사용하세요.
검색된 문서에 특정 종목만 포함되어 있으면 "검색된 문서는 ○○ 중심"이라고 범위를 밝히되, 사용자가 요청하지 않은 미검색 종목·피어·경쟁사를 임의로 언급하거나 "제공되지 않았다"고 나열하지 마세요.
여러 종목을 정리할 때는 아래 구조를 우선 사용하세요.
- `### 종목명 (발간일) [문서번호]`
- `리포트 제목:`
- `핵심 분석:`
- `투자 포인트:`
- `리스크/확인 포인트:`

[관련 리포트 내용]
{context}

[질문]
{question}

[답변]"""

FALLBACK_PROMPT = """주어진 리포트 데이터베이스에서는 사용자의 질문에 대한 관련 문서를 찾을 수 없습니다.
절대로 훈련된 일반적인 지식이나 추측으로 답변하지 마세요. 
오직 '제공된 데이터베이스에서 관련된 문서를 찾을 수 없습니다.'라는 취지로 정중하게 안내하는 답변만 1~2문장으로 출력하세요.

[질문]
{question}

[답변]"""
# 현재 미사용: VectorDB 노드는 자체 no-result 메시지를 반환합니다.

STOCK_PRICE_PROMPT = """당신은 주식 시장 가격 데이터를 바탕으로 사용자의 질문에 답변하는 금융 전문 AI 어시스턴트입니다.
아래 제공된 [주가 정보 데이터]를 바탕으로 사용자의 [질문]에 상세하고 명확하게 답변해 주세요.

[주가 정보 데이터]
{stock_price_data}

[질문]
{question}

[답변]"""
# 현재 미사용: 주가 도구 응답은 stock_price 노드/도구 경로에서 직접 처리합니다.

HISTORY_USAGE_DECISION_PROMPT = """당신은 금융 리포트 챗봇의 대화 맥락 사용 여부를 판단하는 분류기입니다.
이전 대화 맥락(chat_history)과 현재 질문(question)을 보고, 현재 질문을 데이터베이스 검색어로 만들 때 이전 대화의 회사명·종목명·리포트명·목록을 이어받아야 하는지 판단하세요.

[판단 기준]
- TRUE: 현재 질문만으로 검색 대상이 부족하고, 이전 답변/질문에서 나온 대상, 회사, 종목, 리포트, 목록, 비교 대상 등을 이어받아야 자연스럽게 답할 수 있습니다.
- TRUE: 현재 질문이 날짜/기간/조건만 제시하고 주제·섹터·종목이 생략되어 있어 이전 주제를 유지해야 합니다.
- FALSE: 현재 질문에 새로운 회사명, 종목명, 섹터명, 테마명, 리포트명 또는 충분히 독립적인 검색 주제가 명확히 있습니다.
- FALSE: 이전 대화와 무관하게 새 검색으로 처리해도 사용자의 의도가 보존됩니다.
- 애매하면 FALSE를 선택해 이전 맥락을 과도하게 섞지 마세요.

출력은 반드시 TRUE 또는 FALSE 한 단어만 쓰세요.

[이전 대화 맥락]
{chat_history}

[현재 질문]
{question}

판단:"""

QUERY_REWRITE_PROMPT = """당신은 전문적인 수준의 금융 AI 검색 도우미입니다.
사용자의 이전 대화 맥락(chat_history)과 현재 질문(question)을 보고, 데이터베이스 검색에 바로 사용할 수 있는 명확하고 구체적인 검색용 질문으로 재작성하세요.

[중요 지침]
- 이 프롬프트는 이미 이전 맥락이 필요하다고 판단된 질문에만 호출됩니다.
- 현재 질문이 이전 답변의 대상, 회사, 종목, 리포트, 목록, 비교 대상 등을 암시하지만 현재 질문 안에 명시적인 대상명이 없으면 이전 대화에서 가장 최근에 언급된 대상들을 사용하세요.
- 이전 대화에서 여러 회사/종목이 언급되었고 현재 질문이 "회사들", "종목들", "목록", "비교 대상"처럼 복수 대상을 요구하면 가능한 한 해당 대상들을 모두 검색어에 포함하세요.
- 단순히 "특정 리포트 내용 알려줘"처럼 쓰지 말고, 대상 기업명/종목명과 제목·이슈의 핵심 키워드를 포함하여 Vector DB 의미 검색에 친화적으로 작성하세요.
- 현재 질문의 요청 의도(요약, 투자 포인트, 리스크, 비교, 기간 필터 등)는 유지하세요.
- 현재 질문에 새로운 회사명/섹터명/테마명/종목명이 명확히 있으면 이전 대화의 대상을 강제로 섞지 말고 현재 질문의 주제를 우선하세요.
- 현재 질문이 broad search이면 특정 기업명 하나로 좁히지 마세요.

출력은 무조건 재작성된 텍스트 1줄이어야 하며, 군더더기나 설명은 절대 붙이지 마세요.

[이전 대화 맥락]
{chat_history}

[현재 질문]
{question}

검색용 쿼리:"""
